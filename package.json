{
  "name": "domain-drop-watcher",
  "version": "0.1.0",
  "type": "module",
  "private": false,
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "setup": "bash ./scripts/setup.sh",
    "setup:email": "bash ./scripts/setup.sh --email",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "vitest": "^1",
    "wrangler": "^3"
  }
}
