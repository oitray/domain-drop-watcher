# Sender address for email alerts. Must be @ a domain you've enabled Cloudflare Email Routing on. Leave blank if not using email alerts.
ALERT_FROM_ADDRESS=
# Comma-separated hostname globs for the webhook SSRF allowlist (optional — defaults to Teams/Slack/Discord)
WEBHOOK_HOST_ALLOWLIST=
